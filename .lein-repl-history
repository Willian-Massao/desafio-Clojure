(require '[datomic.core :as db])
(def conn (db/init-db))
(require '[datomic.core :as db])
(def conn (db/init-db))
(def uri "datomic:free://localhost:4334/my-db")
(d/connect uri)
(def uri "datomic:free://localhost:4334/my-db")
(d/connect uri)
(:require [datomic.api :as d])
